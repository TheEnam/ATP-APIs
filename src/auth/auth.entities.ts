export type CreateAccountParams = {
    fullname: string;
    email: string;
    password:string;
    userAgent?: string;
}

export type LoginParams = {
    email: string;
    password:string;
   
}

